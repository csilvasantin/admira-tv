const {test}=require('node:test'),assert=require('node:assert/strict'),vm=require('node:vm'),fs=require('node:fs'),path=require('node:path');
const code=fs.readFileSync(path.join(__dirname,'../../js/hidden-ui.js'),'utf8');
function setup(){const events={},classes=new Set(),sent=[];const frame={contentWindow:{postMessage:m=>sent.push(m)}};const window={addEventListener:(n,f)=>events[n]=f,dispatchEvent:()=>{}};window.parent=window;const document={querySelectorAll:()=>[frame],documentElement:{classList:{toggle:(c,on)=>on?classes.add(c):classes.delete(c)}}};vm.runInNewContext(code,{window,parent:window,document,location:{origin:'https://admira.tv'},addEventListener:window.addEventListener,Event:class{}});return{events,classes,sent,frame};}
function key(extra={}){return {key:'h',target:{},preventDefault(){},stopImmediatePropagation(){},...extra};}
test('H toggles twice, preserves typing, modifiers and held-key state',()=>{const s=setup();for(const extra of [{repeat:true},{ctrlKey:true},{target:{isContentEditable:true}},{target:{closest:()=>({})}}])s.events.keydown(key(extra));assert.equal(s.classes.size,0);s.events.keydown(key());assert.ok(s.classes.has('ui-hidden'));s.events.keydown(key({key:'H'}));assert.equal(s.classes.size,0);assert.equal(s.sent.length,2);});
test('cross-frame toggles only accept the current same-origin photo frame',()=>{const s=setup();const data={channel:'admira-hidden-ui-v1',type:'toggle'};s.events.message({origin:'https://evil.invalid',source:s.frame.contentWindow,data});s.events.message({origin:'https://admira.tv',source:{},data});assert.equal(s.classes.size,0);s.events.message({origin:'https://admira.tv',source:s.frame.contentWindow,data});assert.equal(s.classes.size,1);});
